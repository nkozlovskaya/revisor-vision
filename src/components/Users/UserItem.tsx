import { FC, memo } from "react";
import './UsersList.css';

interface UsersItemProps{
    className?: string;
};

export const UsersItem: FC = memo((props: UsersItemProps) => {
    return (
        <div >

        </div>
    );
})